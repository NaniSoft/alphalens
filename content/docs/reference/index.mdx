---
title: Reference
description: Feed field reference and glossary — the exact shapes and terms used everywhere else in these docs.
---

Two reference pages:

- **[Feed field reference](/docs/reference/feed-fields)** — every column of
  `data_feed_view`, which sources populate it, and what the nulls mean.
- **[Glossary](/docs/reference/glossary)** — the terms this site uses, defined
  once.

<Note status="Scope">
The reference documents the **contract's** shape, which is approved, and the
**collector's** captured fields, which are live. It does not document a
backtesting, discovery or validation API, because none exists.
</Note>
